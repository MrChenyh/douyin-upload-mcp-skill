These placeholder files exist only so task-schema validation and dry-run planning can run in a fresh clone.

Do not use them for real publishing. Replace `placeholder-video.mp4` and `placeholder-cover.png` with real media before passing `--execute`.
